//import cron from 'node-cron';
import { logger } from '../utils/logger';

export function registerCronJobs() {
  // TODO: schedule expiry cleanup, low-stock, etc.
  logger.info('Cron jobs registration stub');
}
